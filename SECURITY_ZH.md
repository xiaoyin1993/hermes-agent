# Hermes Agent 安全策略

本文档概述了 **Hermes Agent** 项目的安全协议、信任模型及部署加固指南。

## 1. 漏洞报告

Hermes Agent **不设** 漏洞赏金计划。安全问题应通过 [GitHub 安全公告 (GHSA)](https://github.com/NousResearch/hermes-agent/security/advisories/new) 提交，或发送邮件至 **security@nousresearch.com**。请勿就安全漏洞创建公开 Issue。

### 提交须包含以下信息
- **标题与严重程度：** 简要描述及 CVSS 评分/等级。
- **受影响组件：** 精确的文件路径和行号范围（例如 `tools/approval.py:120-145`）。
- **环境信息：** `hermes version` 输出、commit SHA、操作系统及 Python 版本。
- **复现步骤：** 基于 `main` 分支或最新发布版本的逐步概念验证（PoC）。
- **影响范围：** 说明哪个信任边界被突破。

---

## 2. 信任模型

核心假设是：Hermes 是一个由**单一可信操作者**控制的**个人代理**。

### 操作者与会话信任
- **单租户：** 系统保护操作者免受 LLM 操作的影响，而非防范恶意的共同租户。多用户隔离必须在操作系统/主机层面实现。
- **网关安全：** 授权的调用方（Telegram、Discord、Slack 等）享有同等信任级别。会话密钥用于路由分发，而非授权边界。
- **执行环境：** 默认为 `terminal.backend: local`（直接在宿主机上执行）。容器隔离（Docker、Modal、Daytona）为可选的沙盒方案。

### 危险命令审批
审批系统（`tools/approval.py`）是核心安全边界。终端命令、文件操作及其他潜在破坏性操作在执行前均需经过用户明确确认。审批模式可通过 `config.yaml` 中的 `approvals.mode` 进行配置：
- `"on"`（默认）— 提示用户审批危险命令。
- `"auto"` — 在可配置的延迟后自动批准。
- `"off"` — 完全禁用审批门控（应急模式；参见第 3 节）。

### 输出脱敏
`agent/redact.py` 会从所有显示输出中剥离类似密钥的模式（API 密钥、令牌、凭据），防止其到达终端或网关平台。这可以避免在聊天记录、工具预览和响应文本中意外泄露凭据。脱敏仅在显示层生效——底层数值在代理内部操作中保持不变。

### 技能 vs MCP 服务器
- **已安装的技能：** 高信任级别。等同于本地宿主机代码；技能可以读取环境变量并执行任意命令。
- **MCP 服务器：** 较低信任级别。MCP 子进程接收经过过滤的环境变量（`tools/mcp_tool.py` 中的 `_build_safe_env()`）——仅传递安全的基线变量（`PATH`、`HOME`、`XDG_*`）以及在服务器的 `env` 配置块中显式声明的变量。宿主机凭据默认被剥离。此外，通过 `npx`/`uvx` 调用的包在启动前会经过 OSV 恶意软件数据库的检查。

### 代码执行沙盒
`execute_code` 工具（`tools/code_execution_tool.py`）在子进程中运行 LLM 生成的 Python 脚本，且从环境变量中剥离了 API 密钥和令牌，以防止凭据被窃取。只有已加载技能通过 `env_passthrough` 显式声明的环境变量，或用户在 `config.yaml` 中通过 `terminal.env_passthrough` 配置的环境变量才会被传递。子进程通过 RPC 访问 Hermes 工具，而非直接 API 调用。

### 子代理
- **禁止递归委派：** `delegate_task` 工具在子代理中被禁用。
- **深度限制：** `MAX_DEPTH = 2`——父代理（深度 0）可以派生子代理（深度 1）；孙代理会被拒绝。
- **记忆隔离：** 子代理以 `skip_memory=True` 运行，且无法访问父代理的持久化记忆提供者。父代理仅接收任务提示和最终响应作为观察结果。

---

## 3. 不在范围内（非漏洞）

以下场景**不被**视为安全漏洞：
- **提示注入：** 除非它导致了对审批系统、工具集限制或容器沙盒的实际绕过。
- **公开暴露：** 在没有外部认证或网络防护的情况下将网关部署到公共互联网。
- **可信状态访问：** 需要预先拥有对 `~/.hermes/`、`.env` 或 `config.yaml` 写入权限的报告（这些是操作者所有的文件）。
- **默认行为：** 当 `terminal.backend` 设置为 `local` 时的宿主机级命令执行——这是文档明确记载的默认设置，而非漏洞。
- **配置权衡：** 有意使用的应急设置，例如在生产环境中使用 `approvals.mode: "off"` 或 `terminal.backend: local`。
- **工具级别的读取/访问限制：** 代理通过 `terminal` 工具拥有不受限制的 shell 访问权限，这是设计如此。如果某个特定工具（如 `read_file`）能够访问某个资源，而相同的访问权限也可通过 `terminal` 获得，则不构成漏洞。工具级别的拒绝列表只有在终端侧配有同等限制时才构成有意义的安全边界（如写操作中，`WRITE_DENIED_PATHS` 与危险命令审批系统配合使用）。

---

## 4. 部署加固与最佳实践

### 文件系统与网络
- **生产环境沙盒化：** 对不可信工作负载使用容器后端（`docker`、`modal`、`daytona`）而非 `local`。
- **文件权限：** 以非 root 用户运行（Docker 镜像使用 UID 10000）；在本地安装中通过 `chmod 600 ~/.hermes/.env` 保护凭据。
- **网络暴露：** 不要在没有 VPN、Tailscale 或防火墙保护的情况下将网关或 API 服务器暴露到公共互联网。所有网关平台适配器（Telegram、Discord、Slack、Matrix、Mattermost 等）默认启用 SSRF 防护，包含重定向验证。注意：本地终端后端不应用 SSRF 过滤，因为它运行在可信操作者的环境中。

### 技能与供应链
- **技能安装：** 安装第三方技能前，请审查技能守卫报告（`tools/skills_guard.py`）。`~/.hermes/skills/.hub/audit.log` 中的审计日志会记录每次安装和移除操作。
- **MCP 安全：** 在 MCP 服务器进程启动前，`npx`/`uvx` 包会自动经过 OSV 恶意软件检查。
- **CI/CD：** GitHub Actions 通过完整的 commit SHA 进行固定。`supply-chain-audit.yml` 工作流会阻止包含 `.pth` 文件或可疑的 `base64`+`exec` 模式的 PR。

### 凭据存储
- API 密钥和令牌应**仅**存放在 `~/.hermes/.env` 中——切勿放入 `config.yaml` 或提交到版本控制系统。
- 凭据池系统（`agent/credential_pool.py`）负责密钥轮换和回退。凭据从环境变量中解析获取，不以明文形式存储在数据库中。

---

## 5. 披露流程

- **协调披露：** 90 天窗口期，或直到修复发布，以先到者为准。
- **沟通渠道：** 所有更新通过 GHSA 讨论串或 security@nousresearch.com 的邮件往来进行。
- **致谢：** 除非报告者要求匿名，否则将在发布说明中给予致谢。
